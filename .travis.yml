language: generic 
sudo: required
services:
 - docker

before_install:
 - docker build -t nikolamirkovic/docker-react -f Dockerfile.dev .

 script:
 - docker run -e CI=true nikolamirkovic/docker-react npm run test 